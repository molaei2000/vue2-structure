// initial state

const state = () => ({});

// getters
const getters = {};

// mutations
const mutations = {};

// actions
const actions = {};

export default {
  namespaced: true,
  name: "upcomings",
  state,
  getters,
  actions,
  mutations,
};
